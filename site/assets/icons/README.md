# Navigation icons

Reference SVG paths from the ERP-installed @heroicons/react outline set (24px).
These match the module icons in ERP components/layout/navigation.ts.
Keep the MIT attribution in HEROICONS-LICENSE.txt. No runtime icon CDN or new dependency.
Product adapters retain their icon library; ASIG defines icon size, label, tone and state.
